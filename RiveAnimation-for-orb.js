    RiveAnimation.asset(
      'assets/focium_orb.riv',
      stateMachines: const ['OrbMachine'],
      onInit: (artboard) {
        // Get controller and hook to a double input: audioLevel
      },
    )